# Changelog

## 2026-07-11

- 快捷键被钉钉等软件占用时，不再要求用户更换快捷键；系统热键注册失败后自动启用低级键盘钩子兜底监听，并尽量拦截同组合键。
- 截图工具栏新增“裁剪”功能，框选后直接裁剪当前截图，支持撤销，浮窗会跟随裁剪后图片尺寸调整。
- OCR 结果框根据识别出的文字自动扩展到可读尺寸，避免小框截图后文字被遮住。
- OCR 识别改为多版式模式候选（PSM 6/4/11），按行数、有效字符和乱码惩罚选择最佳结果，减少多段短文本漏识别。
- OCR 翻译入口合并为一个“翻译”按钮，下拉选择中文、英文、德文；翻译后再次点击可恢复原文。
- OCR 文本框拖动和边缘缩放逻辑优化：初始不固定大窗口，拖动边缘时浮窗跟随扩展。
- 增加快捷键兜底、裁剪、OCR 自动放大、OCR 质量和截图缩放等回归测试。

## 2026-07-06

- 修复安装包覆盖旧版本失败的问题：Install.bat 会先关闭正在运行的 ScreenshotHotkeyTool，再复制新版，确保德语翻译按钮生效。

## 2026-07-05


- 修复 OCR 提取文字后点击翻译，按钮显示乱码/问号的问题；内联 OCR 翻译按钮、复原按钮和失败提示改为 Unicode 文案。
- 修复透明浮窗下截图和 OCR 内容空白、只显示工具栏的问题：截图内容和 OCR 文字改由浮窗父窗口直接绘制。
- 默认隐藏截图/OCR 子控件，只保留交互数据；进入标注工具时再显示编辑画布，避免透明窗体绘制异常。
- 修复截图后进入框选/画笔等标注工具点不到截图的问题，截图编辑画布保持可见并接收鼠标事件。
- 修复德语翻译按钮从“复原原文”恢复后显示问号的问题，按钮文字改用 Unicode 转义生成。

## 2026-07-04

- OCR 结果区改为自绘文字框，修复识别后文字不可见的问题。
- OCR 文字框改为强制绘制白底、黑色边框和黑字，并增加像素级测试，避免只显示工具栏不显示文字。
- OCR 结果浮窗支持像截图一样拖动整个窗口；边缘/角落仍可缩放文字框。
- OCR 翻译新增德语入口，Google/Baidu 翻译源均支持德语目标语言。

## 2026-06-29

- 修复截图浮窗只能移动不能缩放的问题：现在拖动截图边缘或角落可放大/缩小，拖动截图内部仍可移动位置。
- 新增截图缩放行为测试，覆盖边缘缩放和内部移动两个场景。
- 修复截图工具栏在小截图区域下右侧按钮被裁掉的问题，浮窗会按工具栏宽度自动扩展。
- 修复 OCR 文字提取框只能缩放不能移动的问题，现在拖动框内可移动，拖动边缘仍可缩放。
- 修复截图/OCR 结果浮窗阻止操作其他页面的问题：窗口改为非模态显示，透明空白区域不再拦截底层页面点击。

## 2026-06-28

- OCR 结果窗口背景恢复透明，只保留文字框、缩放点和工具栏可见。
- 截图编辑工具栏补回“复制”按钮，可直接复制当前编辑后的截图且不关闭窗口。

## 2026-06-27

- 截图快捷键改为原地编辑：框选后直接在截图区域上显示悬浮工具栏，不再弹出独立预览窗口。
- OCR 识别文字快捷键也改为同样的原地编辑流程，框选后可在悬浮工具栏点击识别文字。
- 识别文字后不再弹窗，结果直接显示在当前暗色截图层的选区内，底部切换为 OCR 操作栏。
- OCR 结果框右下角新增缩放手柄，可手动拉大/缩小；文字会按当前框宽度自动换行。
- OCR 结果框支持从边缘和角落拖拽缩放，并会预留更大的浮动区域方便拉大。
- 截图完成后切换为全局透明浮动层，不再显示黑色背景；透明区域露出当前页面并允许继续操作其他窗口。
- 点击截图时显示相关工具，点击其他位置截图不会消失。
- 浮动截图边缘新增黑色描边和浅色内边线，方便区分截图和背景页面。
- 截图框选后未选择工具时，可拖动截图移动位置。
- 悬浮工具栏新增框选、箭头、画笔、文字、序号、马赛克、OCR、撤销、保存、取消、完成等操作。
- 新增样式栏，可切换线条粗细和标注颜色。
- 点击完成会复制编辑后的截图到剪贴板。
- OCR 结果区改为自绘白底黑字大字号区域，增加黑色边框，避免透明浮动窗中识别文字不可见。
- 截图后的透明浮动层改为置顶显示，避免点击落到底层窗口导致工具栏或 OCR 结果异常。

## 2026-06-23

- ???????? OCR????????????????? 300 DPI???????????????????????
- OCR ???? Tesseract LSTM ??????????????????
- ???????????????????????? `Google` / `Baidu`????????????
- ??????????????????????????????
- ???????????`??????????`?
- ?????????????????????
- ?????? 8 ????????????
- ????????????`Google` / `Baidu`?
- ???????? App ID ?????????? Google ??????????

## 2026-06-22

- OCR ????? `??` ? `??`?
- ??????????????????????
- ??????????????????????????
- ????????????? `Tesseract-OCR\tesseract.exe`?????????? Tesseract?
- ???????????????? `Install.bat` ????????
